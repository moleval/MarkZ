# Техническое задание: MARKZ — маркировка заполнений витража

**Редакция:** Ред. 40  
**Файл:** `D:\MARKZ.lsp` (в репозитории: `MARKZ.lsp`)  
**Команды AutoCAD:** `MARKZ`/`МАРКАРОВКА`, `MARKAR`/`МАРКАРЯД`, `MARKTABLE`/`МАРКАТАБЛ`, `MARKA`/`МАРКА`

---

## 1. Общее назначение

Модуль AutoLISP для AutoCAD выполняет три связанные операции над блоками заполнений в витражах:

| # | Команда | Назначение |
|---|---------|------------|
| 1 | **МАРКАРОВКА** | Запись атрибута **«Марка»** в блоки «Заполнение в витраж» |
| 2 | **МАРКАРЯД** | Рядовка условных обозначений — вставка блоков «Ряд заполнений» |
| 3 | **МАРКАТАБЛ** | Ведомость: таблица AutoCAD / XLS / CSV |
| 4 | **МАРКА** | Сквозной цикл: [Сетка/Заполнения] → МАРКАБЛОК → МАРКАРОВКА → МАРКАРЯД → МАРКАТАБЛ, **один UNDO** |
| 5 | **МАРКАБЛОК** (`MARKFILL`) | Автоматическая генерация блоков «Заполнение в витраж» по ячейкам: сетка / точка / полилинии |
- Список: `[Сетка-мультилинии/Точка-мультилинии/3-Сетка-динамика/4-Точка-динамика/Полилинии] <Сетка-мультилинии>`. Два ключа на одну букву нельзя. `Точка-динамика` — команда, либо `4`, либо `Д`. `Сетка-динамика` заполняет только замкнутый контур между разными блоками.
- Извлечение отрезков: LINE / LWPOLYLINE / POLYLINE / MLINE / ARC; лог типов в консоль. Полилинии в режиме «Сетка» игнорируются во избежание захвата контуров фасада/здания.
- MLINE при g10=1: центральная ось через AABB (`GetBoundingBox`); размеры ячейки и оси узлов округляются до 1 мм.
- Т-образные стыки стоек/ригелей: автоматическое объединение суб-ячеек над оканчивающимися снизу стойками в единый световой проём.
- Контроль наклона стоек: строгий допуск `*mark:fill-slope*` = 0.0033 (не более 10 мм на 3000 мм высоты).
- Режим «точка» без замкнутой полилинии: окно `*mark:fill-window*`=5000 мм → сетка → минимальная ячейка под точкой.
- Исключение сквозных зазоров: ячейки не генерируются в пустых проёмах между стойками при разрыве витража.
- Очистка атрибутов: атрибуты обнуляются сразу после установки dynamic W/H, повторно после всех вставок и после EndUndo.

---

## 2. Имена объектов чертежа (точное совпадение)

| Роль | Имя |
|------|-----|
| Заполнение | `Заполнение в витраж` |
| Атрибуты витража | `Атрибуты витража` |
| Атрибут марки | `Марка` |
| Атрибут витража | `Витраж` |
| Ширина (DP) | `Ширина в свету` |
| Высота (DP) | `Высота в свету` |
| Блок рядовки | `Ряд заполнений` |
| Атрибут рядовки | `Ряд` |
| Visibility | `Видимость1` (+ кандидаты в `*mark:prop-vis-candidates*`) |

Если Visibility называется иначе — добавить имя в `*mark:prop-vis-candidates*` (TEST 07).

---

## 3. МАРКАРОВКА — формула марки

```
Марка = [Витраж + " "] + БУКВА(высота) + НОМЕР(ширина) + СУФФИКС
```

- **Буква** — индекс уникальной высоты. Сначала 29 букв списка, дальше `АА`, `АБ`, … `БА`, `ББ`. Маркировка не останавливается.
- **Номер** — `index(ширина) + 1` в `*mark:widths*`. Горизонтальная рядовка.
- Проверка: ширины 900/1200/1500 → 1/2/3; высоты 600/1400/2100 → А/Б/В; 900×2100 → `В1`.
- **Суффикс** (без дефиса, нижний регистр):

| Visibility | Суффикс |
|------------|---------|
| Стемалит | `стм` |
| Сэндвич | `снд` |
| прочее | *(нет)* |


- **Й** и **З** не используются в алфавите (`*mark:letters*`).
- Буквы всегда **заглавные** (`strcase` **без** `T` — в AutoLISP `T` = нижний регистр).
- Одна и та же ширина/высота (допуск 0.5 мм, `mark:same-num?`) → один базис `буква+номер`; stem/сэнд отличаются **только суффиксом**.

### Этапы МАРКАРОВКА

1. **Этап A (без изменения чертежа):** выбор → чтение → TEST 01–13 → расчёт марок → `[DIAG]` базисов → PRE-CHECK (TEST 14).
2. **Этап B:** запись `Марка` **только** если PRE-CHECK = OK.
3. Отмена — один шаг UNDO (в пакете МАРКА — внешний маркер).

---

## 4. МАРКАРЯД — рядовка

После МАРКАРОВКА по выбранным заполнениям:

1. Чтение геометрии и W/H (те же ключи, что в МАРКАРОВКА).
2. **Индекс ряда — только от размера** (`mark:ar-reindex`), не от строки Марки. Разбор Марки — только WARN.
3. **Линия 0** — сплошная, по геометрии позиции; интервал `[pos − L/2 ; pos + L/2]` (без +25), касание = можно.
4. **Линии 1+** — наложения: сортировка **по убыванию длины**, first-fit; сдвиг `k × 150` мм (`*mark:ar-line*`) — «липнут».
5. **Размер dynamic-свойства блока ряда:** `L/2 + 25` мм (осевая по стойкам/ригелям, блок тянется в обе стороны).
6. Горизонталь: rot **270°** (в радианах для `vla-InsertBlock`), атрибут `Ряд` = номер (ширина).  
   Вертикаль: rot 0°, `Ряд` = буква (высота, заглавная); в **`Ширина`** блока пишется `H/2 + 25`.
7. Группы AutoCAD: `Рядовка горизонтальная`, `Рядовка вертикальная`.
8. Старая рядовка в полосе этой раскладки удаляется (`mark:ar-erase-old`) и ставится заново. `mark:ar-exists` остаётся защитой от дубля на той же точке.

**Константы:** offset/gap 1000, line 150, tol 1.0.

**Упаковка (пример):** 1550, 590, 530, 460, 320 → ряд0: 1550; ряд1: 590+530+320; ряд2: 460.

---

## 5. МАРКАТАБЛ — ведомость

- Заголовок: **«Заполнение»** (жирный + подчёркивание).
- Колонки: `№ | Тип | Марка | Высота, мм | Ширина, мм | Кол-во, шт. | Площадь, м2`.
- Размеры: припуск `*mtab:allowance*` = **+26 мм**, округление `fix()`.
- Подитоги по типу + один итог `Итого:` (жирный, подчёркивание на подписи; числа вправо).
- **CSV не в меню** — только тихий фолбэк, если XLS не создался (`mtab:export-xls` → nil → `mtab:export-csv`).
- Формулы XLS: один примитив `mtab:cell`, R1C1, значения 1:1 с LISP.

**Меню:** «Создать таблицу AutoCAD? [Y/N]», «Экспорт в XLS? [Y/N]».

---

## 6. МАРКА — универсальный пакет

```text
МАРКА → Режим [Сетка/Заполнения] <Сетка> → StartUndo → (МАРКАБЛОК) → МАРКАРОВКА → МАРКАРЯД → МАРКАТАБЛ → EndUndo
```

- При выборе `Сетка` (по умолчанию):
  1. Запускается раскладка заполнений по сетке витража (`mark:fill-mode-grid`).
  2. Свежевставленные блоки автоматически передаются в `МАРКАРОВКА` без ручного перевыделения (`*mark:reuse-sel* = t`).
  3. Выполняется маркировка (`МАРКАРОВКА`), построение рядовки (`МАРКАРЯД`) и ведомости (`МАРКАТАБЛ`).
- При выборе `Заполнения`: классический пайплайн по уже существующим на чертеже блокам.
- `*mark:batch-undo* = t`: единая транзакция отката для всех 4 этапов.

---

## 7. Самодиагностика TEST 00–14

| TEST | Содержание |
|------|------------|
| 00 | Баланс скобок исходника |
| 01 | Блоки заполнений |
| 02/03 | Атрибуты витража |
| 04–06 | DP ширина/высота, значения |
| 07 | Visibility |
| 08 | Атрибут «Марка» |
| 09–11 | Уникальные размеры, буквы, номера |
| 12 | Специальные Visibility |
| 13 | Формирование марок в памяти |
| 14 | PRE-CHECK |

**[DIAG]** — группировка по (W,H)±0.5 мм: один базис = OK; конфликт = разные фактические размеры.

---

## 8. Ограничения реализации (нельзя ломать)

- Нет `stringp`, `vla-get-*`/`vla-put-*` как основы — `mark:strp`, `vlax-get/put-property`, `vlax-invoke-method`.
- Нет `let`/lambda-only как основного стиля — `setq`/`progn`/`foreach`.
- Нет `vlax-vla-object-available-p`, `vlax-safearray-get-ubound`.
- `write-line` ровно 2 аргумента, склейка `strcat`.
- `strcase` с `T` = **нижний** регистр; для верхнего — без `T`.
- `vla-InsertBlock` — **радианы**.
- TEST 00 (баланс скобок) обязателен при загрузке.
- Кодировка консоли: без `×` в сообщениях — `x`.
- «Кодируй, но не ломай рабочую программу» — обратная совместимость МАРКАРОВКА/МАРКАТАБЛ/МАРКАРЯД.

---

## 9. Критерии приёмки

1. Загрузка: `[MARKZ] Загружен Ред. 28`, TEST 00 OK.
2. МАРКАРОВКА: 0 критических ошибок; одинаковый размер → один базис; суффиксы `стм`/`снд`. Буква от высоты, номер от ширины (`В1` для 900×2100 при высотах А/Б/В и ширинах 1/2/3).
3. МАРКАРЯД: номера снизу, буквы справа (заглавные); ряд0 сплошной; наложения 150 мм; `Ряд` и `Ширина` заполнены; rot 270°/0°. Старая рядовка зоны удалена.
4. МАРКАТАБЛ: заголовок «Заполнение», колонки по порядку, итоги, XLS или CSV-фолбэк.
5. МАРКА: один выбор, один UNDO.
6. Баланс скобок: `ok`.
