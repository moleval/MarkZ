# AGENTS.md — правила работы с MARKZ.lsp

**Проект:** модуль AutoLISP `MARKZ.lsp` (маркировка / рядовка / ведомость)  
**Источник истины:** рабочий файл `MARKZ.lsp` (поставка: `D:\MARKZ.lsp`)  
**Редакция:** только `*mark:rev*`. Даты и `*mark:build*` нет.  
**Текущая (по факту файла):** Ред. 38

---

## 0. Жёсткие правила

1. **Не ломать рабочие команды** `МАРКАРОВКА`, `МАРКАРЯД`, `МАРКАТАБЛ`, `МАРКА` — «кодируй, но не сломай».
2. После **любой** правки: баланс скобок (TEST 00) должен быть **`ok`**.
3. После правки: `lines` и `bal ok` проверить скриптом; в ответе указать новые line count. Редакция — только `*mark:rev*` (`Ред. N`), без даты и без `*mark:build*`.
4. Правки — точечными заменами по уникальным фрагментам; сверять `assert s.count(old)==1` перед replace.
5. Регулярные выражения/грубые срезы по LISP-литералам **не** применять без проверки — легко отрезать скобки/кавычки.
6. Консоль: **ASCII `x`**, не `×`. Кириллица в сообщениях допустима. `MARKZ.lsp` — **CP1251**, не пересохранять как UTF-8. Документы — UTF-8.

---

## 2. Запрещённый API / стиль

| Нельзя | Вместо |
|--------|--------|
| `consp` (нет в AutoLISP — только `listp`) |
| `stringp` | `mark:strp` |
| `vla-get-*` / `vla-put-*` как основной путь | `mark:ax-get` / `mark:ax-put`, `vlax-get/put-property`, `vlax-invoke-method` |
| `vlax-vla-object-available-p` | не использовать |
| `vlax-safearray-get-ubound` | `vlax-safearray->list` |
| `let`, lambda-only как основной стиль | `setq` / `progn` / `foreach` |
| `write-line` с >2 аргументов | `write-line` (2) + `strcat` |
| `vl-catch-all-apply` c varargs без списка | `(vl-catch-all-apply 'fn (list a b …))` |
| `strcase s T` для **верхнего** регистра | `(strcase s)` — **`T` = нижний** регистр |
| `vla-InsertBlock` rotation в градусах | **радианы**: `(* deg (/ pi 180.0))` |
| Запятые в числах / `×` в выводе | точка; `x` |

`vl-sort` с `' (lambda …)` в **существующем** коде допустим; новый код стараться писать на `foreach`/`vl-sort` без лишних lambda.

---

## 3. Имена и форматы (не переименовывать без ТЗ)

```text
Блок:     Заполнение в витраж | Атрибуты витража | Ряд заполнений
Атрибут:  Марка | Витраж | Ряд
DP:       Ширина в свету | Высота в свету | Видимость1 (+ кандидаты)
Марка:    [Витраж ] + БУКВА(высота) + НОМЕР(ширина) + [стм|снд]
Суффиксы: стемалит → "стм", сэндвич → "снд". Старый список *mark:specials-old* удалён.
Алфавит:  *mark:letters* — 29 букв, без Ё, Й, З, О. Дальше АА, АБ, … БА, ББ. Буквы заглавные
```

**Один базис:** одинаковый W/H (допуск `mark:same-num?` = 0.5 мм) → один `letter`+`hnum`. Stem/сэнд отличаются только суффиксом.

**Рядовка:** индекс — **от размера** (`mark:ar-reindex`), не парсинг строки; парс — только WARN.

---

## 4. Константы рядовки (текущие)

| Переменная | Значение | Смысл |
|------------|----------|--------|
| `*mark:ar-offset*` | 1000 | отступ от фасада |
| `*mark:ar-gap*` | 1000 | (резерв; ряды линий не использовать) |
| `*mark:ar-line*` | 150 | шаг линий 1+ («липнут») |
| `*mark:ar-tol*` | 1.0 | допуск кластеризации |
| размер блока | `L/2 + 25` | dynamic Ширина (обе стороны) |
| rot гориз. | 270° → радианы | `Ряд` = номер (ширина) |
| rot верт. | 0° | `Ряд` = буква (высота); в `Ширина` = `H/2+25` |
| `*mtab:allowance*` | 26 | припуск только в ведомости |
| `*mark:fill-tol*` | 30 | МАРКАБЛОК: выравнивание осей сетки, мм |
| `*mark:fill-slope*` | 0.0033 | МАРКАБЛОК: порог уклона стоек (10 мм на 3000 мм) |
| `*mark:fill-window*` | 5000 | МАРКАБЛОК: окно поиска сетки (режим «точка»), мм |

**Упаковка `mark:ar-pack`:** size ↓, first-fit с линии 0; линия 0 — непрерывная по позиции…  
*Актуальная реализация:* **все** кандидаты size ↓, first-fit в k=0,1,… (не «сначала позиция»). Интервал без +25.

**Группы:** `Рядовка горизонтальная`, `Рядовка вертикальная`.

---

## 5. МАРКА (пакет)

```text
*mark:reuse-sel*  — один ssget на три этапа
*mark:batch-undo* — один StartUndo/EndUndo; внутренние маркеры выключены
```

`mark:reset-state` при `reuse-sel` = t **не** чистит выбор и `*mark:widths*`/`*mark:heights*`.

---

## 6. Обязательные проверки после правки

```bash
# баланс + длина + ключевые маркеры
python3 - <<'PY'
from pathlib import Path
s = Path('MARKZ.lsp').read_bytes().decode('cp1251')
def bal(t):
    d=0; ins=False; com=False; i=0; n=len(t)
    while i<n:
        c=t[i]
        if c=='\n': com=False; i+=1; continue
        if com: i+=1; continue
        if ins:
            if c=='\\': i+=2; continue
            if c=='"': ins=False
            i+=1; continue
        if c==';': com=True
        elif c=='"': ins=True
        elif c=='(': d+=1
        elif c==')': d-=1
        i+=1
    return 'ok' if d==0 and not ins else 'd'+str(d)
print('bal', bal(s), 'lines', s.count(chr(10))+1)
assert bal(s)=='ok'
# точечные assert под вашу правку
PY
```

Минимум:

- [ ] `bal` = `ok`
- [ ] нет случайного `strcase … T` для ВЕРХНЕГО регистра
- [ ] нет `stringp` / `vla-put-` в **новом** коде
- [ ] команды `c:МАРКАРОВКА`, `c:МАРКАРЯД`, `c:МАРКАТАБЛ`, `c:МАРКА`, `c:МАРКАБЛОК` на месте
- [ ] баннер и строка загрузки — только `*mark:rev*` (`Ред. N`), без даты и без `*mark:build*`

---

## 7. Точки расширения

| Задача | Куда |
|--------|------|
| Новое имя Visibility | `*mark:prop-vis-candidates*` |
| Новый суффикс | `*mark:specials*` |
| Новая буква алфавита | `*mark:letters*` и `mark:letter-at` |
| Чтение размера | `mark:get-dyn` + `*mark:prop-*`; фолбэк `mtab:dim` + `*mtab:w-keys*`/`h-keys*` |
| Логика марки | `mark:build-marks`, `mark:test-data` (уникальные W/H) |
| Логика рядовки | `mark:ar-collect`, `mark:ar-reindex`, `mark:ar-pack` |
| Ведомость | `mtab:*` |
| Пакет UNDO/выбор | `mark:a-all`, `*mark:batch-undo*`, `*mark:reuse-sel*` |
| Диагностика базисов | `mark:diag-base` |

---

## 8. Загрузка и приёмка

```lisp
(load "D:/MarkZ/MARKZ.lsp")   ; TEST 00 + [MARKZ] Загружен Ред. N
(МАРКАРОВКА)                ; TEST 01–14, [DIAG], запись
(МАРКАРЯД)                ; рядовка + группы
(МАРКАТАБЛ)             ; таблица / XLS
(МАРКА)                 ; один выбор, один UNDO
```

Приёмка: 0 критических ошибок; `[DIAG] OK`; `Ряд` заполнен; одна строка UNDO на МАРКА.

---

## 9. Связанные файлы

| Файл | Роль |
|------|------|
| `MARKZ.lsp` | **единственный** источник правды |
| `TZ.md` | техзадание |
| `README.md` | пользовательский обзор |
| `AGENTS.md` | этот документ |
| `mark_filling.lsp` | **удалён** — не восстанавливать |
